rename_process("title")
if not hasattr(term.console, "terminal"):
    term.nwrite("\033]0;")
    for pv[get_pid()]["i"] in be.based.user_vars["argj"].split()[1:]:
        term.nwrite(vr("i") + " ")
    term.nwrite("\007")
    be.api.setvar("result", "0")
else:
    be.api.setvar("result", "1")
