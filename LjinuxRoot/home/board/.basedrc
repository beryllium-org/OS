# the splash 
uname -a
cat /etc/motd

# set history size (default 10) to preserve memory
# history-size = 10

# nice aliases
alias ifpexec="fpexec -i"
alias lfpexec="fpexec -l"
alias more="less"
alias ll="ls -l"
alias la="ls -la"
